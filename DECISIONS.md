# 决策日志

## D001 — 2026-09-05：独立仓库

用户明确指定 idler11/MOE。本项目不使用 CCFA/TraceMem 的代码、科学假设或全局指令。仓库由用户创建，研究交接文件由 ChatGPT 初始化。

## D002 — 主科学对象

固定可部署候选集合中，比较 Q0 到 Q1 的有限步功能损失。候选是共享量化权重，不能独立操纵每个 token 的 Top-k。

## D003 — 替代旧梯度叙事

不再以“自动微分遗漏经验目标的普通导数”作为主张。有限校准集在稳定分支邻域内可正确求导；问题是有限离散修改跨越计算分支后的决策失真。总体分布下边界项与有限经验目标必须分开。

## D004 — 优先级

先实现可验证评价仪器，再问收益。P0/P1 首轮授权；P2 及之后通过结果审查逐级开放。不能把单元测试通过当成研究假设通过。

## D005 — 方法 v0

候选坐标一阶/完整二阶近似 + 候选终点的带符号事件项。只在路由条件可精确追踪处使用几何快速路径。先 exact dense oracle，再 event-only 等价实现，再实测成本；不先写 CUDA kernel。

## D006 — 明确不做

不把最优传输损失、保护 router、输出损伤加权、边界平滑、零空间投影单独包装成创新；不进行模型大规模训练；不把 fake quantization 说成真实 INT3/INT4 加速。

## D007 — 原始包保留

四个历史文件 byte-for-byte 保存并校验。原始脚本只在副本执行。旧结果并非留出集或预训练模型实证；零选择改进的对照同样保留。

## D008 — 数据与模型

默认由本项目生成实验数据，generator/seed/family/sequence 切分与哈希可审查。未经批准不拿公开 benchmark 或其他项目数据填充。模型权重作为单独资源在 P3 获批后使用；必须固定 revision 与源实现，不把教程结构当模型事实。

## D009 — 新颖性风险

VSRAQ、GEMQ、SRA-MoE、DREAM-MoE 为直接对照；OPERA-MoE 完整方法尚未取得。2026-08-31 的 Q-Strata 摘要显示其也直接评价组装模型的离散配置，必须排查“候选选择”层面的重叠。未搜索到同名关键词不是新颖性证据。

## D010 — 开放问题

真实模型是否存在非平凡候选机会 V 与基线选择遗憾 R？事件纠偏是否优于同算力直接评估？保留少量 attention/router 高精度是否已足够？能否从局部 W_O 示例扩展到专家权重影响下游路由？这些都是实验门槛，而非已知正面答案。
